apiVersion: "kustomize.toolkit.fluxcd.io/v1"
kind: "Kustomization"
metadata:
  namespace: "flux-system"
  name: "grafana-k6-operator"
spec:
  path: "./grafana-k6-operator"
  prune: true
  interval: "5m"
  sourceRef:
    kind: "GitRepository"
    name: "bootstrap"
