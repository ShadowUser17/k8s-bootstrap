apiVersion: "kustomize.toolkit.fluxcd.io/v1beta2"
kind: "Kustomization"
metadata:
  namespace: "flux-system"
  name: "cert-manager"
spec:
  path: "./cert-manager"
  prune: true
  interval: "2m"
  sourceRef:
    kind: "GitRepository"
    name: "bootstrap"
