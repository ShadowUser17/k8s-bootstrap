apiVersion: "kustomize.toolkit.fluxcd.io/v1"
kind: "Kustomization"
metadata:
  namespace: "flux-system"
  name: "nfs"
spec:
  path: "./nfs-subdir-external-provisioner"
  prune: true
  interval: "5m"
  sourceRef:
    kind: "GitRepository"
    name: "bootstrap"
